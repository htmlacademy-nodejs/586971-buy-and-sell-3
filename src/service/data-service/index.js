'use strict';

const {CategoryService} = require(`./category`);
const {OfferService} = require(`./offer`);
const {CommentService} = require(`./comments`);
const {SearchService} = require(`./category`);

module.exports = {
  CategoryService,
  OfferService,
  CommentService,
  SearchService
};
